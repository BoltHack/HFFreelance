<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>HFFreelance | Ошибка</title>
    <style>
        body {
            font-family: 'Arial', sans-serif;
            background-color: #dadde2;
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
        }

        .container {
            text-align: center;
        }

        .error-box {
            background-color: #fff;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            min-width: 300px;
            max-width: 500px;
            margin: 0 auto;
        }

        .error-box h1 {
            color: #d9534f;
        }

        .error-box h5 {
            color: #5a5a5a;
        }

        .error-box pre {
            background-color: #f8f8f8;
            padding: 10px;
            border-radius: 5px;
            overflow-x: auto;
        }

        .home-link {
            display: inline-block;
            margin-top: 20px;
            padding: 10px 20px;
            background-color: #5cb85c;
            color: #fff;
            text-decoration: none;
            border-radius: 5px;
            transition: background-color 0.3s;
        }

        .home-link:hover {
            background-color: #4cae4c;
        }

    </style>
</head>
<body>
<div class="container">
    <div class="error-box">
        <h2>Ошибка</h2>
        <% if (!error.status) {%>
            <pre><%= error.message %></pre>
        <%} else {%>
                <h1><%= message %></h1>
                <h5>Статус ошибки: <%= error.status %></h5>
                <pre><%= error.message %></pre>
            <%}%>
    </div>
    <a href="/" class="home-link">Вернуться на главную</a>
    <a href="#" class="home-link" onclick="window.history.back()">Назад</a>
</div>
</body>
</html>
