<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>HFFreelancers | Leave a review</title>
    <link rel="stylesheet" href="/stylesheets/sendReviews.css">
    <link rel="icon" type="image/png" href="/images/logo2.png">
    <% if (user && user.id) { %>
        <script>
            const idUser = "<%= user.id %>";
        </script>
    <% } %>

</head>
<body>
<%- include('header.ejs') %>
<main>
    <div class="review-container">
        <h2>Leave Your Review</h2>
        <form action="/sendReviews/<%= user.id %>" method="POST" id="reviewForm">
            <div class="form-group">
                <textarea id="message" name="message" placeholder="Your review" maxlength="150" required style="color: white"></textarea>
                <input id="grade" type="number" name="grade" placeholder="Rating (1-5)" min="1" max="5">
            </div>
            <button type="submit" id="sendReview">Submit</button>
        </form>
    </div>
</main>
</body>
</html>