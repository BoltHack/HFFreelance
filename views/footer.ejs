<link rel="stylesheet" href="/stylesheets/main2.css">
<footer id="footerSection" hidden>
    <div class="footer-container">
        <div class="footer-links">
            <div class="footer-our-company">
                <div style="display: flex">
                    <img src="../images/logo2.png" class="company-logo">
                    <h3 style="margin: 5px 10px;">HFFreelance</h3>
                </div>
                <p>Создание современных сайтов для создания нового источника заработка, смены профессии или карьерного роста.</p>
            </div>
            <div class="footer-info">
                <h3>Информация</h3>
                <a href="/aboutUs">О нас</a>
                <a href="/rules">Правила использования</a>
                <a href="/privacyPolicy">Политика конфиденциальности</a>
            </div>
            <div class="footer-contacts">
                <h3>Контакты</h3>
                <% if (links) { %>
                    <% links.forEach(linksItem => { %>
                        <% if(linksItem.vk) {%>
                            <a href="<%= linksItem.vk %>" target="_blank">VK</a>
                        <%}%>
                            <% if(linksItem.discord) {%>
                                <a href="<%= linksItem.discord %>" target="_blank">Discord</a>
                            <%}%>
                            <% if(linksItem.instagram) {%>
                                <a href="<%= linksItem.instagram %>" target="_blank">instagram</a>
                            <%}%>
                            <% if(linksItem.facebook) {%>
                                <a href="<%= linksItem.facebook %>" target="_blank">facebook</a>
                            <%}%>
                        <% }); %>
                <% } else { %>
                    <p>Новых ссылок пока нет.</p>
                <% } %>
            </div>
        </div>
        <div class="line"></div>
        <p class="company-name">&copy; 2024 HFFreelance. Все права защищены.</p>
    </div>
</footer>
<script src="/javascripts/footer.js"></script>
<script src="/javascripts/script.js"></script>