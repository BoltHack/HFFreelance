<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>HFFreelancers | Оставьте отзыв</title>
    <link rel="stylesheet" href="/stylesheets/sendReviews.css">
    <link rel="icon" type="image/png" href="/images/logo2.png">
    <% if (user && user.id) { %>
        <script>
            const idUser = "<%= user.id %>";
        </script>
    <% } %>
</head>
<body>
<%- include('header.ejs') %>
<main>
    <div class="review-container">
        <h2>Оставьте свой отзыв</h2>
        <form action="/sendReviews/<%= user.id %>" method="POST" id="reviewForm">
            <div class="form-group">
                <textarea id="message" name="message" placeholder="Ваш отзыв" maxlength="150" required></textarea>
                <input id="grade" type="number" name="grade" placeholder="Оценка (1-5)" min="1" max="5">
            </div>
            <button type="submit" id="sendReview">Отправить</button>
        </form>
    </div>
</main>
</body>
</html>