<style>
    .errors{
        text-align: center;
        position: relative;
        top: 50%;
        left: 50%;
        transform: translate(-50%, -50%);
    }
    .errorButton{
        border: none;
        background: none;
        color: #24527a;
        font-size: 15px;
        cursor: pointer;
    }
    .errorButton:hover{
        text-decoration: underline;
    }
</style>
<div class="errors">
    <h2>Ой, кажется, у нас возникли проблемы :(</h2>
    <section id="info">
        <button id="onSec" class="errorButton">Узнать подробнее</button>
        <button id="reload" class="errorButton">Перезагрузить страницу</button>
    </section>
    <section id="section" hidden>
        <h1><%= message %></h1>
        <h5>Статус ошибки: <%= error.status %></h5>
        <pre><%= error.message %></pre>
        <button id="closeInfo" class="errorButton">Закрыть</button>
    </section>
</div>
<script>
    const section = document.getElementById('section');
    const info = document.getElementById('info');
    document.getElementById('reload').addEventListener('click', () => {
        window.location.href = '/'
    })
    document.getElementById('onSec').addEventListener('click', () => {
        section.hidden = false;
        info.hidden = true;
    })

    document.getElementById('closeInfo').addEventListener('click', () => {
        section.hidden = true;
        info.hidden = false;
    })
</script>